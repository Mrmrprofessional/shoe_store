<html>
<head>
    <title>Shoes</title>

<body>
    <h1>{{ brand.getBrand }}</h1>

    {% if stores is not empty %}
        <p>Here are the stores that have this brand:</p>
        <ul>
            {% for store in stores %}
                    <li>{{ store.getName }}</li>
            {% endfor %}
        </ul>
    {% endif %}

    <h4>Add a store to this brand:</h4>

    <form action='/add_store' method='post'>
        <input id="brand_id" name="brand_id" type="hidden" value="{{ brand.getId }}">
        <label for="store_id">Select a store</label>
        <select id='store_id' name='store_id' type='text'>
          {% for store in all_stores %}
            <option value="{{ store.getId }}"> {{ store.getName }} </option>
          {% endfor %}
        </select>
        <button type='submit'>Add store</button>
    </form>
    <p><a href='/'>Home</a></p>
</body>
</html>
